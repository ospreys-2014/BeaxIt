<table class="table table-striped table-hover ">
  <tbody>
    <tr> <td><a href="/track/<%= track.id%>"><%= track.title %></a></td></tr>
    <tr><td>
      <br>
      <audio controls> <source src="tracks/<%=track.title%>.mp3" type= "audio/mp3"></audio>
      </td></tr>
    </tbody>
</table>
<% if session[:user_id] %>
  <%= erb :"playlists/add", locals:{track: track} %>
<% end %>


